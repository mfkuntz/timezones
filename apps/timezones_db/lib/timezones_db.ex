defmodule TimezonesDb do
  @moduledoc """
  TimezonesDb — placeholder for the database-backed timezone storage layer.

  This package will eventually provide a `Calendar.TimeZoneDatabase`
  implementation backed by a database (e.g. Ecto/SQL).
  """
end
